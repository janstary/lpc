#ifndef F2C_INCLUDE
#define F2C_INCLUDE

#include "lpc10.h"

#define TRUE_ (1)
#define FALSE_ (0)

#ifndef min
#define min(a,b) ((a) <= (b) ? (a) : (b))
#endif

#ifndef max
#define max(a,b) ((a) >= (b) ? (a) : (b))
#endif

#endif
